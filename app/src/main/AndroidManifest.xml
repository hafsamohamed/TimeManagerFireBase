<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.timemanagerfirebase">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".notifications.APP"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="TaskPad"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TimeManagerFireBase">
        <activity android:name=".manager.TimeActivity"></activity>
        <activity android:name=".manager.CalenderActivity" />
        <activity android:name=".manager.TaskActivity" />
        <activity android:name=".manager.HomePageActivity" />
        <activity android:name=".login.RegistrationActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".splash.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>